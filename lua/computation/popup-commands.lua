--[[------------------- resolution v0.1.0 -----------------------

this file provides the operations and shell calls for the popup menu

---------------------------------------------------------------]]

local commands = {}

commands.set_keybinds = function(popups)
end

commands.mount = function(initial)
end

commands.mount_from_visual = function()
end

commands.remount = function()
end

commands.remount_from_visual = function()
end

commands.sympy_job = function(input)
end

commands.latex_job = function(input)
end

commands.latex_indent_job = function(input, filename)
end

commands.closure_jobs = function(popups)
end

commands.jobs_creator = function(input)
end

return commands
